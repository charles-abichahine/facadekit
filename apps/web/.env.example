# Copy to apps/web/.env.local for local overrides (gitignored).
# Empty = use the dev proxy at /api. In production Vercel sets this to the
# deployed API's URL, e.g. https://facadekit-api.fly.dev
VITE_API_URL=
